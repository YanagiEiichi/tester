{
  "name": "ui-tester",
  "version": "1.1.4",
  "description": "ui-tester",
  "main": "tester.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/YanagiEiichi/tester.git"
  },
  "bin": {
    "ui-tester-start": "start.js"
  },
  "scripts": {
    "test": "./start.js tests/*",
    "prepublish": "cat node_modules/es6-promise/dist/es6-promise.auto.min.js src/* >> tester.js && uglifyjs tester.js -o tester.js",
    "tag": "git checkout HEAD~0 && git add -f tester.js && git commit -m 'RELEASE' && git tag $(node -pe 'require(\"./package.json\").version') && git checkout -"
  },
  "author": "YanagiEiichi <admin@web-tinker.com>",
  "license": "MIT",
  "devDependencies": {
    "es6-promise": "^4.0.3",
    "uglify-js": "^2.7.3"
  },
  "dependencies": {
    "argollector": "^1.0.0",
    "glob": "^7.1.0",
    "node-static": "^0.7.8"
  }
}
